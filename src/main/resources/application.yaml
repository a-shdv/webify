spring:
  jpa:
    hibernate:
      ddl-auto: create
    repositories:
      enabled: true
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/webify?serverTimezone=UTC
    username: root
    password: root
#  flyway:
#    baseline-on-migrate: true